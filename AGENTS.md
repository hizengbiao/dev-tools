# Dev Tools 项目协作入口

进入本项目的新会话在分析或修改代码前，必须先阅读：

1. `SESSION_CONTEXT_EXPORT.md`：项目架构、用户约定、重点工具设计、测试和 Git 流程。
2. `docs/toolbox-todo.md`：功能清单、完成记录和后续 TODO 的维护规范。

如果文档与当前仓库状态存在差异，以当前代码、测试和 Git 状态为准。开始工作前至少执行：

```powershell
git status --short
git log -5 --oneline
```

## 必须遵守

- 使用中文沟通，需求明确时直接实现，不反复要求确认。
- 不覆盖、撤销或提交用户已有的无关改动。
- 修改完成后运行相关测试和 `git diff --check`。
- 页面功能变更时同步更新版本号和版本更新说明。
- 版本说明必须使用实际修改日期；同一天的版本归入同一个日期标题。
- 完成修改后使用中文且不含日期的提交说明提交，并推送到 `gh/main`，除非用户明确要求不提交或不推送。
- 新增工具时同步更新 `nav.js`、`index.html`、`README.md`、`DEVELOPMENT.md` 和测试。
- 涉及浏览器交互的改动需要进行真实页面验证，不能只做静态文本检查。

## 上下文维护

- `SESSION_CONTEXT_EXPORT.md` 是跨会话摘要。项目架构、长期约定或关键功能状态发生明显变化时，应同步更新。
- 新识别的正式功能项写入 `docs/toolbox-todo.md`；完成后标记为 `[x]` 并补充真实日期的完成记录。
- 不要把细小调整过度拆分成大量 TODO。
